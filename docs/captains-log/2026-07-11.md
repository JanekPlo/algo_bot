# Captain's log — 2026-07-11 (MR-Session 1: Audit)

Session log (working session, not the weekly brain audit). One deliverable: the
mean-reversion audit — MMS extraction + deep references + independent oracle pass.

## Done

- **Task 0 — MMS extraction.** `docs/references/mms/` created: README (copyright /
  not-for-redistribution), 4 HIGH tabs (01 position building, 02 position
  management/filters, 03 stop-loss/sequentiality, 04 interval marking), 2 MEDIUM
  (05 market mechanics, 06 semi-auto algo), 33 full-tab screenshots in
  `raw/mastermind/`. Live site was up (cross-checked tab 01 text verbatim);
  screenshots came from Janek's local captures. **Open:** tab 09 (Backtesty
  2025/2026) — no source screenshots yet; stub to be added when captured.
- **Deep references.** `strategy-mean-reversion-bb-stoch.md` DRAFT→FULL (hybrid;
  taxonomy + watchlist; 16-row alignment table citing mms/ per row; sweep
  interpretation caveat; known limitations incl. the engine-migration early
  warning). New `indicators-bbands.md`, `indicators-stochastic.md`.
- **Independent oracle pass** (lesson 2026-06-11): unpack order, Stoch gate
  placement, same-bar precedence, warmup guard — verified clean. Three uncovered
  state-machine seams mechanised as `TestAuditSeams` (re-touch no-refresh; exit bar
  never arms; both-bands touch arms nothing) + hand-derivable bbands window-2
  identity test. Only code touch: `require_reclaim` docstring drift fix (3 places,
  zero behaviour change).
- **Docs drift fix** (Beta follow-up closed): package-overview, ARCHITECTURE,
  config-reference. `.gitignore`: `*Zone.Identifier`.

## Findings that matter downstream

1. **Stochastic role divergence (deliberate).** MMS: add-on/pyramiding filter,
   %K & %D cross 20/80, 14/3/3 H1, applied with a position open. Beta: %K-only
   base-entry gate at arming, `entry_mode` sweep dimension. Documented as
   adaptation, resolved empirically in MR-Session 2. %D is computed and tested —
   available if cross semantics are ever wanted.
2. **Sweep interpretation caveat.** MMS explicitly claims the bare core loses in
   strong trends and the sequential sizing layer (x1→x0.1 scouts→TP→x1) is the
   edge. MR-Session 2 go/no-go must separate "bare core viable?" from "did we test
   the actual MMS edge?" (we did not — by design). A weak sweep ≠ methodology
   falsified; a strong sweep = pyramiding becomes enhancement, not rescue.
3. **Author's own EA** parametrizes BB(31, 3.0, SL 1.7%) — supports sweeping
   window/num_std/sl_pct instead of freezing 20/2.0/2%.

## Next deliverable

**MR-Session 2 (Sweep):** `mr_b1..b3` × BTC/ETH under `WF_ELIGIBILITY_THRESHOLDS`
(ADR-013) + per-year regime-robustness sanity check (`running-sweep.md`). Watchlist
params (⚠ in the taxonomy) get the neighbourhood-stability read, not best-cell
cherry-picking. VPS runner (Session 4b) available for the queue.

## Operator notes

- `make check` to be run by Janek in WSL (sandbox bash dead over UNC); new tests:
  `TestAuditSeams` ×3, bbands identity ×1. Test C (`both_bands_touch_no_arm`) has a
  fixture-precondition assert — if it trips, pick a different quiet bar `q`.
- Chat-pasted screenshots don't land as files (uploads dir empty) — for the git
  record, PNG must be copied into `docs/references/mms/raw/mastermind/`.
