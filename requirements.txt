# =============================================================================
# requirements.txt — UWAGA: ten plik jest TERAZ generowany przez pip-tools.
#
# NIE EDYTUJ RECZNIE. Zrodlem prawdy dla dependencies jest pyproject.toml.
#
# Aby zregenerowac po zmianie pyproject.toml:
#   make lock     # = pip-compile --extra dev --output-file=requirements.txt pyproject.toml
#
# Aby zainstalowac dokladnie te wersje (deterministic install):
#   make sync     # = pip-sync requirements.txt + pip install -e . --no-deps
#
# UWAGA: TA-Lib NIE bedzie w lockfile — instalowane przez conda-forge
# (patrz environment.yml). `make sync` instaluje tylko pip-deps; TA-Lib
# jest juz w conda env.
#
# Ten plik zawiera obecnie STARA recznie utrzymywana liste (fallback przed
# pierwszym `make lock`). Po `make install` i `make lock` zostanie nadpisany
# precyzyjnym lockfile z transitive deps i version-hashes.
# =============================================================================

# --- LEGACY fallback (do regeneracji przez `make lock`) ----------------------
# Patrz pyproject.toml [project.dependencies] / [project.optional-dependencies]
# dla zrodla prawdy.

# Core data
ccxt>=2.8.58
pandas>=1.5.0
numpy>=1.23.0

# Backtesting
backtesting>=0.3.2
# TA-Lib instalowane przez conda-forge, nie tutaj

# Config + env
PyYAML>=6.0
python-dotenv>=1.0.0

# Time zones
tzdata>=2024.1

# Viz
matplotlib>=3.6.0

# Testing
pytest>=7.4.0
